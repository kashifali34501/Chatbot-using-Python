[project]
name = "openchat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kashifali34501", email = "kashifali34501@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.2",
    "streamlit>=1.64.0",
]

[project.scripts]
openchat = "openchat:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
